#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @h: find absolute value of a number
 *
 * Return: absolute value
 */
int _abs(int h)
{
	if (h < 0)
	{
		return (-h);
	}
	return (h);
}

