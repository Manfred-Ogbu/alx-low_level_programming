#include "main.h"
/**
 * _islower - function that checks for lowercase character
 * @k: test for character
 * Return: returns 1 for True and 0 for false
 */
int _islower(int k)
{
	return (k >= 'a' && k <= 'z');
}

